/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',
                  'www.google.com', 'blogger.googleusercontent.com',
                    'encrypted-tbn0.gstatic.com'],
      },
};

export default nextConfig;
